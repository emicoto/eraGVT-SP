
;--------------------------------
;ラスボス強化、最大HPが５倍に増加する
;--------------------------------
@LASTBOSS_KYOUKA_F
#FUNCTION
IF CONFIG_CHECK_BALANCE_F(5) > 0 && (FLAG:10 == 1)
	RETURNF 5
ELSE
	RETURNF 1
ENDIF

;--------------------------------
;ラスボス強化時、回復量を通常レベルに落とす
;--------------------------------
@LASTBOSS_REST
IF CONFIG_CHECK_BALANCE_F(5) > 0 && FLAG:10 == 1
	LOCAL = 8
ELSE
	LOCAL = 0
ENDIF
RETURN LOCAL
