;**********************************************************
;基本ルール
;加筆修正は、自由に行ってください
;ただし、前の方が書いた文章は出来る限り消さないでください
;
;書き込む順番は、クドくなり過ぎないように3要素と部位で表現します
;【実況】汁に濡れている、寄生されている、触手拘束具などを装着など
;【形状】大きさ、深さなど、
;【動作】開閉している、うごめいているなど、動きを表現
;【部位呼称】淫乱や発情や性格で下品な書き方に
;
;Bust(おっぱい)については、@SLANG_Bを参考
;
;増やして欲しい隠語は、アウトプットしてリクエストなりしましょう
;未登録の中に放り込むなり、掲示板にアップするなりすれば、
;実装される可能性があります
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Nipple(ニップル)の淫語
;**********************************************************
@SLANG_N(ARG)
#DIMS SLANGWORD_N
#FUNCTIONS

;初期化
SLANGWORD_N=

;第一形容詞：状況（汁など）
IF (STAIN:2 & 4)
	SLANGWORD_N+="精液を塗られた"
ENDIF

;第二形容詞：形状表現


;第三形容詞：動作表現
IF TALENT:淫乳
	SLANGWORD_N += "快楽にそそり勃つ"
ENDIF

;部位呼称
IF TALENT:淫乳
	SLANGWORD_N += "乳芽"
ELSE
	SELECTCASE RAND:5
		CASE 4
			SLANGWORD_N += "乳頭"
		CASE 3
			SLANGWORD_N += "胸の先端"
		CASE 2
			SLANGWORD_N += "乳先"
		CASE 1
			SLANGWORD_N += "乳首"
		CASEELSE
			SLANGWORD_N += "ニップル"
	ENDSELECT
ENDIF

RETURNF SLANGWORD_N


;*************【淫語覚書（未登録）】***********************
;
;
;*************【淫語覚書（未登録）】***********************



