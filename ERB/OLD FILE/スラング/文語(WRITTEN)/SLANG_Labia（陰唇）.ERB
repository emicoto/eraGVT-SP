;**********************************************************
;基本ルール
;加筆修正は、自由に行ってください
;ただし、前の方が書いた文章は出来るだけ消さないでください
;
;書き込む順番は、クドくなり過ぎないように3要素と部位で表現します
;【実況】汁に濡れている、寄生されている、触手拘束具などを装着など
;【形状】大きさ、深さなど、
;【動作】開閉している、うごめいているなど、動きを表現
;【部位呼称】淫乱や発情や性格で下品な書き方に
;
;大陰唇%SLANG_LabiaMajora(TARGET)%と
;小陰唇%SLANG_LabiaMinora(TARGET)%で更に小分けされている
;
;
;増やして欲しい隠語は、アウトプットしてリクエストなりしましょう
;未登録の中に放り込むなり、掲示板にアップするなりすれば、
;実装される可能性があります
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Labia(陰唇)の淫語
@SLANG_Labia(ARG)
#DIM LCOUNT
#DIMS SLANGWORD_Labia
#FUNCTIONS

;初期化
SLANGWORD_Labia=


;1/3で大陰唇だけ、1/3で大陰唇だけ、1/3で大陰唇・小陰唇両方を
LCOUNT=RAND:99
IF LCOUNT > 65
	;女性器を隠す皮下脂肪の多い部分、外側のワレメのこと
	SLANGWORD_Labia=%SLANGWORD_Labia%%SLANG_LabiaMajora(TARGET)%
ELSEIF LCOUNT >32
	;女性器の膣穴を隠す部分、内側のビラビラのこと
	SLANGWORD_Labia=%SLANGWORD_Labia%%SLANG_LabiaMinora(TARGET)%
ELSE
	;女性器を隠す皮下脂肪の多い部分、外側のワレメのこと
	SLANGWORD_Labia=%SLANGWORD_Labia%%SLANG_LabiaMajora(TARGET)%

	SELECTCASE RAND:2
		CASE 1
			SLANGWORD_Labia+="に囲まれた"
		CASEELSE
			SLANGWORD_Labia+="の内側に"
	ENDSELECT

	;女性器の膣穴を隠す部分、内側のビラビラのこと
	SLANGWORD_Labia=%SLANGWORD_Labia%%SLANG_LabiaMinora(TARGET)%
ENDIF


;合成して出来上がった淫語を返す
RETURNF SLANGWORD_Labia




