;**********************************************************
;基本ルール
;加筆修正は、自由に行ってください
;ただし、前の方が書いた文章は出来る限り消さないでください
;
;口述での「尻」なので、吐息や詰まった表現などはありです
;体力や発情など、ステータスでどもったりする様にしましょう
;この変数を呼び出すときは、吃ることも含めてお使いください;
;尻穴以外の尻肉を表現する
;
;増やして欲しい隠語は、アウトプットしてリクエストなりしましょう
;未登録の中に放り込むなり、掲示板にアップするなりすれば、
;実装される可能性があります
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Hip(尻)の淫語
;**********************************************************
@S_SLANG_H(ARG)
#DIMS S_SLANGWORD_H
#FUNCTIONS

;初期化
S_SLANGWORD_H=


;部位呼称
IF TALENT:淫尻>0
	S_SLANGWORD_H += "淫尻"
ELSEIF TALENT:淫乱>0
	SELECTCASE RAND:3
		CASE 2
			S_SLANGWORD_H += "りんご尻"
		CASE 1
			S_SLANGWORD_H += "桃尻"
		CASEELSE
			S_SLANGWORD_H += "尻鞠"
	ENDSELECT
ELSE
	SELECTCASE RAND:3
		CASE 3
			S_SLANGWORD_H += "尻の谷間"
		CASE 2
			S_SLANGWORD_H += "ヒップ"
		CASE 1
			S_SLANGWORD_H += "尻肉"
		CASEELSE
			S_SLANGWORD_H += "お尻"
	ENDSELECT
ENDIF

RETURNF S_SLANGWORD_H


;*************【淫語覚書（未登録）】***********************
;巨尻
;
;
;
;
;*************【淫語覚書（未登録）】***********************



