;搾り取る
@COM104

;距離表示
CALL PRINT_DISTANCE
PRINTL 

;油断度の上昇
TFLAG:3 += 80 + 20 * ABL:技巧

;防御フラグ：搾り取るにする
TCVAR:2 = 体勢：搾り取る

;地の文：搾り取る
CALL MESSAGE_BATTLE_CHARA_SEX_ATTACK104
;地の文：洗脳/悪堕ちキャラが操作キャラの搾り取るを見た
SIF FLAG:110 == 1
	CALL MESSAGE_OTHER_BATTLE_CHARA_SEX_ATTACK104

;射精ゲージを記録
LOCAL:1 = FLAG:15

;射精
LOCAL = 225 + ABL:技巧 * ABL:技巧 * SQRT(FLAG:15 / 2 + 50)
CALL TENTACLE_SYASEI_UP, LOCAL

PRINTL
IF (FLAG:110 == 0)
	CALL TENTACLE_ACCESS, "NAME"
ELSEIF (FLAG:110 == 1)
	PRINTFORM %PRINT_TRANSCALLNAME(FLAG:111)%
ENDIF
PRINT に
FONTBOLD
PRINTFORM {FLAG:15 - LOCAL:1}の快楽
FONTREGULAR
PRINTL を与えた！
PRINTW 

;触手の射精チェック
CALL TENTACLE_SYASEI_CHECK
PRINTL 

;習得の獲得
COMMON_PALAM:6 += 450 + 25 * (ABL:従順 + ABL:奉仕精神 * 2)

;行動ポイントを+1
EX:行動ポイント += 1

RETURN 1
